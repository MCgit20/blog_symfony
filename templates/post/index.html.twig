{% extends 'base.html.twig' %}

{% block title %}Gestion des Articles{% endblock %}

{% block body %}
    <div class="container">
        <div class="d-flex justify-content-between align-items-center mb-4">
            <h1>📝 Gestion des Articles</h1>
            <a href="{{ path('app_post_new') }}" class="btn btn-success">+ Nouveau Article</a>
        </div>

        {% if posts is empty %}
            <div class="alert alert-info text-center">
                Aucun article trouvé.
            </div>
        {% else %}
            <div class="table-responsive">
                <table class="table table-striped table-hover">
                    <thead class="table-dark">
                        <tr>
                            <th>ID</th>
                            <th>Titre</th>
                            <th>Catégorie</th>
                            <th>Contenu (aperçu)</th>
                            <th>Publié le</th>
                            <th>Actions</th>
                        </tr>
                    </thead>
                    <tbody>
                    {% for post in posts %}
                        <tr>
                            <td><small>{{ post.id }}</small></td>
                            <td><strong>{{ post.title }}</strong></td>
                            <td><span class="badge bg-primary">{{ post.category.name }}</span></td>
                            <td><small>{{ post.content|slice(0, 50) }}...</small></td>
                            <td>{{ post.publishedAt ? post.publishedAt|date('d/m/Y') : '-' }}</td>
                            <td>
                                <a href="{{ path('app_post_show', {'id': post.id}) }}" class="btn btn-sm btn-info">👁️ Voir</a>
                                <a href="{{ path('app_post_edit', {'id': post.id}) }}" class="btn btn-sm btn-warning">✏️ Éditer</a>
                            </td>
                        </tr>
                    {% endfor %}
                    </tbody>
                </table>
            </div>
        {% endif %}

        <a href="{{ path('app_home') }}" class="btn btn-secondary mt-3">← Retour</a>
    </div>
{% endblock %}
