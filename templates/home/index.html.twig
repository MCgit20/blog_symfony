{% extends 'base.html.twig' %}

{% block title %}Accueil - Mon Blog{% endblock %}

{% block body %}
    <div class="jumbotron bg-light p-5 rounded-3 mb-4">
        <h1 class="display-4">Bienvenue sur Mon Blog ! 🌟</h1>
        <p class="lead">Découvre les derniers articles sur la technologie et bien plus encore.</p>
    </div>
    <h2 class="mb-4">Derniers Articles</h2>
    {% if posts is empty %}
        <div class="alert alert-info">
            Aucun article publié pour le moment. Revenez bientôt !
        </div>
    {% else %}
        <div class="row">
                    {% for post in posts %}
                <div class="col-md-6 col-lg-4 mb-4">
                    <div class="card h-100 shadow-sm">
                        {% if post.picture %}
                            <img src="{{ asset('uploads/' ~ post.picture) }}" class="card-img-top" alt="{{ post.title }}">
                        {% endif %}
                        <div class="card-body">
                            <h5 class="card-title">{{ post.title }}</h5>
                            <p class="card-text">{{ post.content|slice(0, 150) }}...</p>
                            <div class="d-flex justify-content-between align-items-center">
                                <small class="text-muted">{{ post.publishedAt|date('d/m/Y') }}</small>
                                <a href="{{ path('post_show', {'id': post.id}) }}" class="btn btn-sm btn-primary">Lire plus</a>
                            </div>
                        </div>
                    </div>
                </div>
            {% endfor %}
</div>
{% endif %}
{% endblock %}